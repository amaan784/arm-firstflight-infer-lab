# Structured benchmark results (JSON) are written here by `firstflight bench`.
# Clearly-synthetic samples (example_*.json) are committed so the repo looks complete
# before anyone runs it; real runs exclude them automatically.
